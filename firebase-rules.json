{
  "rules": {
    "claims": {
      ".read": true,
      "$area": {
        ".write": "auth != null && ((!data.exists() && newData.hasChildren(['owner','deviceId','userId','updatedAt','timestamp']) && newData.child('userId').val() == auth.uid) || (data.exists() && !newData.exists() && data.child('userId').val() == auth.uid))",
        ".validate": "!newData.exists() || (newData.child('owner').isString() && newData.child('owner').val().length <= 32 && newData.child('deviceId').isString() && newData.child('userId').isString() && newData.child('userId').val() == auth.uid && newData.child('updatedAt').isNumber() && newData.child('timestamp').isNumber())"
      }
    }
  }
}
